# Session 2026-09-04 — Babyfoot au boulot

> Résumé de la session du 4 sept 2026 — `dev` → `github.com:pierrecahuzac/babyfootauboulot.git`

## Où on en était
- **Repo** créé `pierrecahuzac/babyfootauboulot`, branche `dev` = `main` `5df241a` → `fc791e7`
- **Infra** `docker-compose.yml` 4 services `db:5432` `api:33333` `web:55174` `landing:55175` + `Caddyfile.example`
- **DB** `users`/`players`/`ligues`/`ligue_members`/`matches` avec `initDb` + migration `Boulot`

## Fait aujourd'hui
1. **Initial commit + push dev** (`5df241a` → `dev`/`main`, `.gitignore`, `origin` `git@github.com:pierrecahuzac/babyfootauboulot.git`)
2. **Auth v2** (`api/src/app.js`, `utils/auth.js`, `utils/moderation.js`):
   - `register`/`login`/`me`/`logout` + `verify-email`/`resend`/`forgot`/`reset` + `PATCH /me`/`change-password`/`DELETE /me`
   - Validation `isValidEmail`, rate-limit `5/15min 429`, JWT `HS256` + `JWT_SECRET` fail-closed, tokens hashés `sha256` dev-only leak
3. **Drizzle Studio dev-only** (`drizzle.config.js`, `docker-compose.yml:71` `profiles dev`, `docker-compose.prod.yml`, `api/Dockerfile` `npm install`, `.env.example` `STUDIO_PORT`)
4. **Thème sombre** (`web/src/App.jsx:23` `dark` par défaut, toggle `🌙/☀️`, `index.css` `@custom-variant dark`, `dark:` partout, fix admin lisible `04d63a0`)
5. **Sécurité** (`documentation/SECURITE.md` 3 niveaux 🔴/🟠/🟢, `CORS` whitelist, `helmet`, `env_file + :?` fail-fast, `Caddy` ordre `handle`)
6. **Nettoyage MVP/IP** (`App.jsx:234` `MVP`→`Sécurisé`, `api/app.js` commentaires, `ETAT_DES_LIEUX` `192.168`→`réseau local`, `docker-compose.yml:27`)
7. **Vitrine** (`landing/src/pages/index.astro` retire stack `Postgres/Fastify/React`, ports, `MVP`, `Hébergé chez vous`, footer `© 2026`)
8. **Seed** (`api/src/db/seed.js`, `db:seed`/`db:seed:clean`): `32 users` (30 + `admin@example.com`/`admin1234` `admin` + `demo@example.com`/`demo1234` `user`) + `5 ligues` + `120 matchs` (24/ligue, scores `10-0` à `10-9`)
9. **Rôles** (`schema.js:18` `role admin/user`, `middleware/auth.js` `requireAdmin`, `routes/admin.js` `GET/PATCH/DELETE /api/admin/users`, `Admin.jsx` tri + badge)
10. **Modération** (`moderation.js` blocklist `hitler`… leet `4→a`, hook `register`/`PATCH`/`players`/`ligues`)
11. **Claim invité → compte** (`POST /api/players/:id/claim` `routes/players.js:75`, transfert `team_bleue/rouge` + delete `players`)
12. **MVC** (`api/src/app.js 902→151` + `middleware/auth`, `utils/helpers`, `routes/auth/ligues/players/matches/admin`, `web/src/App.jsx 988→194` + `12` pages `web/src/pages/*`, tests `api 24` `web 21` pass, `docker compose build`)

## Comptes démo
- `admin@example.com` / `admin1234` → `admin` système (pas joueur, `players` exclu)
- `demo@example.com` / `demo1234` → `user` (joueur)
- Tous les autres `*@example.com` / `demo1234`

## Commandes
```bash
docker compose up -d --build
docker compose --profile dev up -d studio # https://local.drizzle.studio
docker compose exec api npm run db:seed:clean # 32 + 5 + 120
docker compose exec api npm run test # 24 passed
cd web && npm run test # 21 passed
```

## À faire avant prod (SECURITE.md)
- P0 restants: `isMember` fail-closed, `UPDATE` whitelist, `helmet` prod, `httpOnly` cookie, CSP `index.html`, Caddy `HSTS`, `USER node`, `.dockerignore` `web/landing`
- Prod `.env` sur serveur (jamais commité), `CORS_ORIGIN`, `ADMIN_EMAILS=ton@mail.fr` (remplace `admin@example.com`)

## Reprise demain
Dis `on en était où ?` → je relis `ETAT_DES_LIEUX.md`, `SECURITE.md`, `git log dev` et ce `SESSION_2026-09-04.md`.
